set(IGNORE Darwin NetBSD)
if("${BENCHAMEL_SYSTEM_NAME}" IN_LIST IGNORE)
  message(STATUS "Skipping concurrency-bugs on ${BENCHAMEL_SYSTEM_NAME}")
  return()
endif()

if(POLICY CMP0169)
  cmake_policy(SET CMP0169 OLD)
endif()

add_bug(aget-bug2)
add_bug(cherokee-bug1)
add_bug(httrack-3.43.9)
add_bug(memcached-127)
add_bug(pbzip2-0.9.4)
add_bug(stringbuffer-jdk1.4)
add_bug(transmission-1.42 DEFAULT ON)
