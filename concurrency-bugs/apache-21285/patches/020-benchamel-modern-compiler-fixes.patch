# Source: Benchamel local compatibility patch, based in part on the original
# Apache-21285 installation notes for newer x86_64 toolchains.
--- a/srclib/apr/misc/unix/errorcodes.c
+++ b/srclib/apr/misc/unix/errorcodes.c
@@ -359,7 +359,7 @@ static char *gaerror(apr_status_t statcode, char *buf, apr_size_t bufsize)
 }
 #endif
 
-#if defined(HAVE_STRERROR_R) && defined(STRERROR_R_RC_INT) && !defined(BEOS)
+#if defined(HAVE_STRERROR_R) && (defined(STRERROR_R_RC_INT) || defined(__linux__)) && !defined(BEOS)
 /* AIX and Tru64 style */
 static char *native_strerror(apr_status_t statcode, char *buf,
                              apr_size_t bufsize)
--- a/srclib/apr-util/include/apr_optional.h
+++ b/srclib/apr-util/include/apr_optional.h
@@ -102,8 +102,8 @@ APU_DECLARE_NONSTD(void) apr_register_optional_fn(const char *szName,
  * @param name The name of the function
  */
 #define APR_REGISTER_OPTIONAL_FN(name) \
-    (((void (*)(const char *, APR_OPTIONAL_FN_TYPE(name) *)) \
+    (((void (*)(const char *, apr_opt_fn_t *)) \
                &apr_dynamic_fn_register)(#name,(apr_opt_fn_t *)(name)))
 
 /** @internal
--- a/srclib/apr-util/include/apr_optional_hooks.h
+++ b/srclib/apr-util/include/apr_optional_hooks.h
@@ -100,8 +100,8 @@ APU_DECLARE(void) apr_optional_hook_add(const char *szName,void (*pfn)(void),
 
 #define APR_OPTIONAL_HOOK(ns,name,pfn,aszPre,aszSucc,nOrder) \
-    ((void (APR_THREAD_FUNC *)(const char *,ns##_HOOK_##name##_t *,const char * const *, \
+    ((void (APR_THREAD_FUNC *)(const char *,void (*)(void),const char * const *, \
 	       const char * const *,int))&apr_optional_hook_add)(#name,(void (*)(void))pfn,aszPre, \
 							   aszSucc, nOrder)
 
